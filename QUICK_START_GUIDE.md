# 🏀 NBA Integrity Guard - 快速参考指南

**最后更新**: 2026-01-31
**版本**: v1.0 - Demo Ready
**状态**: ✅ 完全可用

---

## 🚀 快速启动

### 启动系统（一条命令）

```bash
cd /mnt/d/lebron/cc项目/1/nba-integrity-guard
./start-full-demo.sh
```

### 打开浏览器

```
http://localhost:3000
```

### 停止系统

```bash
./stop-full-demo.sh
```

---

## 📱 系统地址

| 组件 | URL | 说明 |
|------|-----|------|
| **前端仪表板** | http://localhost:3000 | React 美化界面 |
| **Mock API** | http://localhost:3001 | 后端数据服务 |
| **API 健康检查** | http://localhost:3001/health | 服务状态 |

---

## 📊 前端界面说明

### 打开 http://localhost:3000 你会看到：

#### 1️⃣ **Header（导航栏）**
- NBA Integrity Guard 标题
- 深色主题设计
- 底部隐藏交互（点击版权处可切换模式）

#### 2️⃣ **Integrity Dashboard（主仪表板）**
三列布局：

**左列：Live Feed**
- 实时数据流显示
- 市场动态更新
- 交易事件推送

**中列：Main Analysis（宽屏）**
- 核心分析面板
- 图表和数据展示
- 异常检测结果

**右列：Execution Log**
- 执行日志记录
- 系统事件追踪
- 性能指标监控

#### 3️⃣ **背景效果**
- 点阵网格背景（1% 透明度）
- 环境光晕渐变（SAFE/CRITICAL 模式）
- 平滑动画过渡

---

## 🎮 交互操作

### 模式切换
```
点击页面底部版权处 (©)
→ 在 SAFE 和 CRITICAL 模式之间切换
→ 查看界面颜色和动画变化
```

### Live Feed 交互
- 实时数据自动更新
- 支持滚动查看历史
- 可展开查看详情

### 图表交互
- 鼠标悬停显示数值
- 支持缩放和平移
- 点击图例切换数据

---

## 📡 API 端点文档

### 获取市场数据
```bash
curl http://localhost:3001/api/markets
```

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "slug": "will-trump-win-2024",
      "question": "Will Trump win the 2024 election?",
      "outcomes": ["YES", "NO"],
      "volume": 15234567.89,
      "liquidity": 8500000,
      "yesPrice": 0.72,
      "noPrice": 0.28
    }
  ]
}
```

### 获取声誉排行榜
```bash
curl http://localhost:3001/api/reputation/leaderboard
```

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "address": "0x1234...",
      "totalTrades": 45,
      "totalVolume": 850000,
      "winRate": 0.73,
      "reputationScore": 85,
      "badges": ["Oracle", "Whale"]
    }
  ]
}
```

### 获取交易者档案
```bash
curl http://localhost:3001/api/reputation/profile/0x1234567890123456789012345678901234567890
```

### 获取交易者统计
```bash
curl http://localhost:3001/api/reputation/stats/0x1234567890123456789012345678901234567890
```

### 获取声誉评分
```bash
curl http://localhost:3001/api/reputation/score/0x1234567890123456789012345678901234567890
```

### 获取徽章列表
```bash
curl http://localhost:3001/api/reputation/badges/0x1234567890123456789012345678901234567890
```

### 获取交易历史
```bash
curl http://localhost:3001/api/reputation/history/0x1234567890123456789012345678901234567890?limit=10
```

### 获取市场情绪
```bash
curl http://localhost:3001/api/analytics/sentiment/will-trump-win-2024
```

---

## 📝 查看日志

### 查看实时日志（持续输出）
```bash
# Mock API 日志
tail -f logs/mock-api.log

# React 前端日志
tail -f logs/frontend.log
```

### 查看最后 100 行
```bash
tail -100 logs/mock-api.log
tail -100 logs/frontend.log
```

---

## 🛠️ 常见问题

### Q: 前端无法加载？
**A**:
1. 检查是否在 http://localhost:3000（不是 3001）
2. 检查 Vite 是否运行：`ps aux | grep vite`
3. 查看日志：`tail -f logs/frontend.log`

### Q: API 返回 404？
**A**:
1. 确保 Mock API 服务器运行中
2. 检查端口是否为 3001
3. 查看日志：`tail -f logs/mock-api.log`

### Q: 数据不更新？
**A**:
1. 这是 Mock 数据，不会实时变化
2. 刷新页面可以看到新的随机数据
3. 生产环境会连接真实数据库

### Q: 如何修改 Mock 数据？
**A**: 编辑 `/mock-server/server.js`
```javascript
// 修改 const traders = [ ... ] 的内容
// 修改 const markets = [ ... ] 的内容
// 然后重启服务
```

---

## 📂 项目文件结构

```
/nba-integrity-guard/
├── mock-server/              # Mock API 服务器
│   ├── server.js             # 400+ 行，包含所有 API 端点
│   └── package.json
│
├── frontend-web/             # React 前端
│   ├── src/
│   │   ├── App.tsx           # 主应用组件
│   │   ├── index.tsx         # 入口
│   │   ├── components/       # 组件库
│   │   │   ├── Header.tsx
│   │   │   ├── LiveFeed.tsx
│   │   │   ├── MainAnalysis.tsx
│   │   │   ├── ExecutionLog.tsx
│   │   │   ├── GlassCard.tsx
│   │   │   └── VisualEffects.tsx
│   │   ├── context/          # 全局状态
│   │   │   └── SimulationContext.tsx
│   │   └── types.ts
│   ├── index.html
│   ├── vite.config.ts
│   ├── tsconfig.json
│   └── package.json
│
├── backend/                  # 后端微服务（可选升级）
│   ├── polymarket-indexer/
│   ├── auth-service/
│   ├── notification-service/
│   ├── analytics-service/
│   ├── reputation-service/
│   └── payment-service/
│
├── start-full-demo.sh        # 启动脚本
├── stop-full-demo.sh         # 停止脚本
└── logs/                     # 日志目录
    ├── mock-api.log
    └── frontend.log
```

---

## 🎯 关键文件说明

### mock-server/server.js
- **大小**: 400+ 行
- **用途**: 提供所有 API 端点，返回模拟数据
- **特点**:
  - 包含 5 个市场数据
  - 包含 5 个交易者信息
  - 支持 9 个 API 端点
  - CORS 启用

### frontend-web/src/App.tsx
- **大小**: 75 行
- **用途**: 主应用布局和路由
- **特点**:
  - 3 列响应式布局
  - 深色主题背景
  - 动态背景效果
  - 模式切换功能

### frontend-web/src/components/MainAnalysis.tsx
- **大小**: 12,000+ 字节
- **用途**: 主分析面板
- **特点**:
  - Recharts 图表集成
  - 实时数据显示
  - 交互式数据探索

---

## 🔄 工作流程

```
用户打开浏览器
    ↓
加载 React 前端 (localhost:3000)
    ↓
前端初始化，获取 SimulationContext
    ↓
加载 Header、LiveFeed、MainAnalysis、ExecutionLog 组件
    ↓
各组件向 Mock API (localhost:3001) 请求数据
    ↓
Mock API 返回模拟数据
    ↓
前端渲染数据并展示
    ↓
用户可交互（切换模式、查看详情等）
```

---

## 🚀 升级路径

### 当前状态（Mock Demo）
- ✅ 完整的前端界面
- ✅ Mock API 服务器
- ✅ 模拟数据
- ❌ 真实数据库

### 升级步骤 1：连接真实数据库
1. 安装 PostgreSQL 或 Docker
2. 运行 `backend/database/` 中的 SQL 文件
3. 启动真实后端服务

### 升级步骤 2：完整微服务
1. 启动 6 个后端服务（3001-3006）
2. 集成所有数据源
3. 部署到生产环境

### 升级步骤 3：完整系统
1. Docker Compose 编排
2. Kubernetes 部署
3. CDN + HTTPS

---

## 📞 支持和反馈

### 查看完整产品报告
见: `PRODUCT_REPORT.md`

### 系统架构文档
见: `SYSTEM_ARCHITECTURE.md`

### 技术文档
见: `TECHNICAL_DOCUMENTATION.md`

---

**需要帮助？查看日志或重启服务！**

```bash
# 重启整个系统
./stop-full-demo.sh && sleep 2 && ./start-full-demo.sh
```

